<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% for item in playgrounds %}
       name={{ item.name }} lat={{ item.X }} lng="{{ item.Y}}"<br/>
    
      {% endfor %}
    </body>
</html>
